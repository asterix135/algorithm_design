'''approaches to multiplying two SQUARE matrices with identical dimensions
Will throw an error if these conditions are not met'''

## setup stuff
import time

def brute_force(x_matrix, y_matrix):
    start = time.time()
    z_matrix = []
    for row in len(x_matrix):
        for element in row:


    print (time.time() - start)
    return z_matrix

